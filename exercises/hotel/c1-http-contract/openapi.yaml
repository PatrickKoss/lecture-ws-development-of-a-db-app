openapi: 3.0.3
info:
  title: Hotel API
  version: 1.0.0
paths:
  /api/room-types:
    get:
      responses:
        '200': {description: Liste der Zimmertyp-Datensätze}
    post:
      responses:
        '201': {description: Angelegt}
        '400': {description: Validierung fehlgeschlagen}
        '409': {description: type_code bereits vorhanden}
