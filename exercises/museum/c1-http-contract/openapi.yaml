openapi: 3.0.3
info:
  title: Museum API
  version: 1.0.0
paths:
  /api/exhibits:
    get:
      responses:
        '200': {description: Liste der Exponat-Datensätze}
    post:
      responses:
        '201': {description: Angelegt}
        '400': {description: Validierung fehlgeschlagen}
        '409': {description: inventory_code bereits vorhanden}
