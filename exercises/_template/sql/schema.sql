PRAGMA foreign_keys = ON;

-- TODO(domain): Lege hier das normalisierte Schema mit 4 bis 6 Entitäten an.
-- Jede Tabelle braucht einen Primary Key. Setze alle fachlich nötigen Foreign
-- Keys, NOT NULL- und UNIQUE-Constraints. Die n:m-Beziehung wird als eigene
-- Tabelle mit ihren Beziehungsattributen modelliert. Das Skript muss mit
-- SQLite 3 in einer leeren Datenbank laufen.
