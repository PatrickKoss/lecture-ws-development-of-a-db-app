openapi: 3.0.3
info:
  title: Paketdienst API
  version: 1.0.0
paths:
  /api/parcels:
    get:
      responses:
        '200': {description: Liste der Paket-Datensätze}
    post:
      responses:
        '201': {description: Angelegt}
        '400': {description: Validierung fehlgeschlagen}
        '409': {description: tracking_code bereits vorhanden}
