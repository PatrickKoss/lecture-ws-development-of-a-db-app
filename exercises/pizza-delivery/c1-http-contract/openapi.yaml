openapi: 3.0.3
info:
  title: Pizza-Lieferdienst API
  version: 1.0.0
paths:
  /api/pizzas:
    get:
      responses:
        '200': {description: Liste der Pizza-Datensätze}
    post:
      responses:
        '201': {description: Angelegt}
        '400': {description: Validierung fehlgeschlagen}
        '409': {description: pizza_number bereits vorhanden}
