openapi: 3.0.3
info:
  title: Liefermarktplatz API
  version: 1.0.0
paths:
  /api/restaurants:
    get:
      responses:
        '200': {description: Liste der Restaurant-Datensätze}
    post:
      responses:
        '201': {description: Angelegt}
        '400': {description: Validierung fehlgeschlagen}
        '409': {description: partner_number bereits vorhanden}
